# P3-84 CoDriving TVM 执行边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批 12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的只读审查。三项双审一致；九项经第三次只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 3 |
| P6 执行契约 | 9 |

三项 P4 规范历史架构审计、诊断报告或 INT8 来源证明等外部证据制品。九项 P6 自身执行 ONNXRuntime/TVM 导入、build、编译、调优、虚拟机运行、数值对齐、计时或能耗测量并写出新结果。第三方裁决确认：外部 ONNX、校准或参考制品属于输入约束，不改变脚本自身执行职责；一项 OpenCOOD 标记仅指形状/配置来源，静态未见明确代码复制或移植证据。

没有运行候选、模型、数据、设备、子进程或网络任务。受限 evidence 和 HMAC 不进入公开树；本批新增 12 个身份后真实唯一覆盖为 **782/1,551**。
