# P3-21：外部执行脚本逐项复审

状态：已完成（本地）。本批从未审计的非测试 Python 代码中，以外部输入线索建立 12 项队列；之后逐项阅读模块职责、导入、调用、输入/输出、公开同路径状态、来源历史和安全风险。线索只用于选队列，未直接决定处置。独立抽样复审覆盖 P4、P6 和两项路径阻塞，未发现需撤回的结论。

## 逐项结论

| 职责类别 | 数量 | 暂定处置 | 复审结论 |
| --- | ---: | --- | --- |
| Orin/H800 构建、测量、AP bridge、Stage6 控制与表格收口脚本 | 7 | `execution_contract_p6` | 每项实际消费 checkpoint、数据、编译制品、GPU/TensorRT/TVM 运行时或正式 evidence 行，并产生测量、AP、收口或 provenance 输出；公开树不存在等价安全执行器。 |
| Stage2 量化覆盖/anchor 生成和原生 INT8 AP 报告导入 | 3 | `external_contract_p4` | 主导职责是读取/验证 measured LUT 行、外部 AP 报告及制品溯源字段；必须先由 P4 建立许可、版本、checksum、预期目录和路径安全投影。 |
| CoDriving TRT/TVM AP bridge | 2 | `blocked_license_or_permission` | 两个源文件均硬编码用户专属仓库根路径，不能原样公开；P7 必须完成环境变量化、泄露回归和重新审查后才能重新进入 P4--P6。 |

有一处静态凭据赋值模式命中经 AST/上下文复核后确认只是结构化载荷的局部 `token` 变量，不包含密钥、环境读取或网络凭据；其仍因 checkpoint、数据、engine 和硬件依赖进入 P6。其余扫描未发现邮箱、IPv4 或凭据赋值。

本批没有运行私有脚本、模型、数据、GPU、TensorRT/TVM、子进程或真实制品，也没有迁入任何公开文件。每一项都记录了无法安全运行的具体理由；P4--P6 后续必须以受控输入和失败关闭验证替代本批的静态/语义审阅。

## 来源、证据与阶段边界

12 项皆为已跟踪的维护者控制源码，逐项 P2 metadata/字节数与当前内容 SHA-256 已在受限记录中对账；历史作者与当前维护者配置一致，且没有嵌入式第三方许可证/来源标记。P2b inventory 没有每文件历史内容 digest，因此源变化时必须重新审计。

本批的 12 条本机 HMAC records 不是关闭账本；P3 新标准累计数从 25/1,551 增至 37/1,551。P4 仍未启动。
