# P3-43 Stage5/Stage6 执行与外部证据边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4 启动授权。

## 范围与结论

本批从冻结 P2b 基线中确定性选取 12 个、且不在此前 278 个唯一已覆盖身份中的代码候选。每项完成 P2 metadata/bytes、当前内容 SHA-256、职责/输入/输出/调用者、公开职责反证、许可证/来源和安全输出的只读检查。所有当前内容绑定均通过。

主审和独立语义复审对 10 项结论一致。另有两项的主导边界出现 P4/P5/P6 分歧，第三次只读裁决确认其 GPU 状态检查和反馈证据处理均服务于真实验证或反馈轮次的执行编排，而非独立的环境声明或仅外部输入 schema，故均归入 P6。

| 处置 | 数量 | 主导责任 |
| --- | ---: | --- |
| P4 | 4 | ONNX 与 checkpoint/source registry、终端摘要和 native AP 外部证据契约 |
| P6 | 8 | 性能任务计划、后处理闭环、独立验证、原子反馈与轮次控制执行契约 |

## 许可与安全边界

候选源码可验证地继承项目级 Apache-2.0，未见独立第三方许可证或来源标记；因此没有因源码许可证本身进入 `blocked_license_or_permission`。该结论不覆盖数据、权重、checkpoint、ONNX、engine、校准缓存、GPU 环境或测量制品，它们仍由 P4/P6 的后续契约处置。

候选的错误、manifest、日志和证据对象可能携带制品位置、设备分配或结果引用。后续公开实现必须改用路径无关标识、环境变量化配置和失败关闭；不得迁入原始本机默认值或真实外部资产。

## 验证边界

本批只读取源码、调用关系和公开树反证；没有运行候选代码、私有模型、数据、设备、子进程或网络任务。受限逐路径 evidence、decisions 和 HMAC key 不进入公开树；路径无关 HMAC ledger 已由当前 inventory 与 decisions 重算。该批新增 12 个不重复身份后，P3 的真实唯一覆盖为 **290/1,551**。
