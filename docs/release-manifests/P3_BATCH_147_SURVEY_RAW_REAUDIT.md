# P3-147 Survey raw、D-space 与协同工作流复审

状态：已接受（本地，含 P7 阻塞）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批 12 项 `migrate_document` 候选完成 P2 分类/字节数、当前内容 SHA-256、实际职责与输入输出、调用关系、公开等价物、来源/许可证和安全输出的双重逐项审查；主审与独立复审逐项一致。最终为 P4=9、P6=2、P7=1，无重复项。

- P4：survey_raw 文献检索/摘要、耦合分类、D-space 研究和 survey_raw_2 索引属于外部文献与 provenance 证据。
- P6：survey_task_plan 与协同加速框架工作流定义调查任务、调用链和执行步骤。
- P7：`d_space_nvidia.md` 含 `/tmp/nvidia-mps` 私有临时运行路径；须参数化、清除私有运行标识并完成当前树/历史/导出物扫描后重审。

未运行候选、模型、数据、GPU、网络或外部服务；受限 evidence、逐项 decisions、候选 identity 与 HMAC key 不进入公开树。

本批新增 12 个冻结身份后，真实唯一覆盖为 **1,532/1,551**，剩余 **19** 项。P3 仍在进行中，P4 尚未开始；未推送、未发布。
