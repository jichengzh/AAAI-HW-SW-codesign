# P3-102 量化、评测与推理边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的双独立只读审查。主审与独立复审均先核验同一固定队列身份；7 项经第三方只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 6 |
| P6 执行契约 | 6 |

P4 项限定量化 manifest、私有计划测试、指标报告、ONNX 图转换或 evidence 制品。P6 项实际反序列化 TensorRT engine、加载/转换 checkpoint、构建数据集评测、导出 ONNX、执行分布式推理或模型前向测时。第三方裁决拒绝了两个缺少真实公开实现与测试链的“已改写”提议；仅有环境依赖也不足以将实际检查、评测、导出或测时工具降为 P5。没有运行候选、模型、数据、设备、子进程或网络任务；受限 evidence 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **998/1,551**。
