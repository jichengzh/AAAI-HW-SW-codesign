# P3-138 论文方法与评测协议复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批 12 项 `migrate_document` 候选完成 P2 metadata/bytes、当前内容 SHA-256、职责/I-O/调用关系、公开等价物、来源/许可证与安全输出的双重逐项审查，并经第三方逐项裁决。最终 12 项全部为 `external_contract_p4`，reason 为 `paper_source_boundary`。这些文件是论文草稿、方法来源、评测口径和仿真方法学文本，没有被运行时调用的 runner/API、作业队列或执行 I/O；后续需按最终实验账本、代码实现、引用和外部制品许可收口。q6/q7 的技术性评测/仿真定义不改变其论文 source boundary 主导职责，因此不升 P6。

未发现真实凭据、主机/IP、令牌或可直接执行的访问秘密；未运行候选、模型、数据、GPU、网络或外部服务。受限 evidence、逐项决策、候选 identity、HMAC key 不进入公开树。

本批新增 12 个冻结身份后，真实唯一覆盖为 **1,424/1,551**，剩余 **127** 项。P3 仍在进行中，P4 尚未开始；未推送、未发布。
