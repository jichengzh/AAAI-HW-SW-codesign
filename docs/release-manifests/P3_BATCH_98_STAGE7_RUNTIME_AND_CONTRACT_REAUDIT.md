# P3-98 Stage7 运行时与契约边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的双独立只读审查。主审与独立复审均先核验同一固定队列身份；6 项经第三方只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 2 |
| P5 环境契约 | 2 |
| P6 执行契约 | 7 |
| 非必要排除 | 1 |

P4 项分别处理 release-asset 和 checkpoint-evaluation 的外部制品边界；P5 项处理 H800 运行时盘点/锁与 core 准备时的 deployment pin；P6 项实际承担在线消融、调度、source lease、materializer 或 measurement-admission 控制。内部一致性分析不在受支持的公开复现链内，按非必要排除。第三方裁决确认两个尚未存在公开实现与测试的候选不能被记为“已迁入”或“已改写”，均保留为 P4。没有运行候选、模型、数据、设备、子进程或网络任务；受限 evidence 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **950/1,551**。
