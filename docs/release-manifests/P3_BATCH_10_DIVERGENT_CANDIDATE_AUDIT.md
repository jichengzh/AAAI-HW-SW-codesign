# P3-10：同路径差异候选逐项审计

状态：已完成本地只读审计及第二个本地账本检查点；未提交私有清单、decisions、HMAC key 或 ledger，未推送、未发布。

## 审计边界与计数漂移

本批次以 P2b 冻结 inventory（SHA-256 `6fafdace7dc0eae3e9982763cb1c5c098c0ae83d1715af67ff1e08d7a1d12a79`）为候选基线，并以当前公开工作树重新进行同一相对位置的内容比对。P3-8 记录的“同路径但内容不同”数量为 38；当前工作树观察到 39。这是公开树在 P3-8 后继续演进所致，不能将旧计数外推到当前 HEAD。

39 项全部逐项进行语义、公开接口、来源/许可标记和私有标识风险审阅；不以目录名、文件后缀或批量规则直接决定。它们由 38 个代码候选和 1 个文档候选组成。审阅结果如下：

| 当前审阅结论 | 数量 | 可否立即形成最终 ledger 决定 |
| --- | ---: | --- |
| 当前公开契约已安全替代私有版本 | 9 | 可以；已写入本地 P3-10 子集。 |
| 需要先完成公开脱敏改写 | 13 | 不可以；须先有测试、公开契约与安全复审。 |
| 需要来源/许可证核查 | 2 | 不可以；须先有再分发权或明确排除依据。 |
| 存在路径、硬件制品、私有标识或等效安全边界风险 | 15 | 不可以；须先确认可否安全改写，否则按阻塞规则处置。 |

后 30 项是审阅状态，不是“已完成”的 ledger disposition；不得为了凑数将它们自动迁入、自动排除或自动标为阻塞。

## 本地 P3-10 账本检查点

其中 9 项由当前公开、已测试的契约安全替代。维护者在仓库外的权限受限目录中为每项写入 `duplicate_or_superseded` 决定，使用固定 `public_contract_supersedes` reason 和 `public_contract_review` evidence token，并以同一个受限 HMAC key 生成 9 条路径无关记录。生成后检查确认记录只含 HMAC-SHA256 candidate ID、P2 分类和受控 audit token；不含路径、origin、正文、人员、机器、地址或 key。

这使本地已完成、可复核的 P3 decisions 累计为 18/1,551：P3-9 的 9 项同内容替代和本批的 9 项契约替代。两个子集仅用于流程检查点，不能拼接为完整 ledger，更不构成 P3 关闭条件。

## 后续顺序

1. 完成 P3-9 的 selection completion、ranking/Pareto 与 uncertainty replay 三个公开纯内存模块，并通过合成测试、安全复审和 Stage4 回归；
2. 对上述 13 项“需要改写”的候选逐项绑定到已经验证的公开模块或新的公开契约后，再写入本地 decisions；
3. 对 2 项来源/许可证候选取得明确依据；对 15 项高风险候选逐项确认安全改写、外部契约、非必要排除或阻塞结论；
4. 继续处理当前无公开同路径的 1,504 项候选。只有 1,551 项全部恰好一条决定、再生成全量路径无关 ledger 并完成同一 HEAD 验收后，P3 才可能关闭。

## 未改变的边界

本批次没有复制私有源码、文档、实验结果、模型、checkpoint、engine、ONNX、校准缓存、硬件日志或服务器配置。任何公开改写仍必须保持纯内存或显式外部契约边界；不得读取或回显维护者路径、URI、运行名、地址、身份或凭据。
