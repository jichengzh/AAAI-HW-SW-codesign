# P3-143 可配置剪枝 Phase A–D 复审

状态：已接受（本地，含 P7 阻塞）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批 12 项 `migrate_document` 候选完成 P2 分类/字节数、当前内容 SHA-256、实际职责与输入输出、调用关系、公开等价物、来源/许可证和安全输出的双重逐项审查，并经第三方逐项只读裁决。最终为 P4=2、P6=8、P7=2，无重复项。

- P4：剪枝最终进展汇总和 `PROGRESS.md` 主要记录已完成测量、engine/Pareto 结果及阶段状态，属于外部结果/provenance 契约。
- P6：Phase A1–A4、B1、C1 以及两个正式实施计划包含剪枝器、配置评估、敏感度扫描、ONNX/TRT 构建、联合压缩管线和验收编排。
- P7：`CLAUDE_Phase_D.md` 与内容相同的 `实施计划_Phase_D.md` 均含绝对本机路径、checkpoint/校准/GPU 命令和私有运行边界。两者虽字节相同，但没有公开实现与测试/API 职责对比，不能记为 `duplicate_or_superseded`；须先脱敏和确认资产许可后重审。

未运行候选、模型、数据、GPU、网络或外部服务；受限 evidence、逐项 decisions、候选 identity 与 HMAC key 不进入公开树。

本批新增 12 个冻结身份后，真实唯一覆盖为 **1,484/1,551**，剩余 **67** 项。P3 仍在进行中，P4 尚未开始；未推送、未发布。
