# P3-80 TVM kernel 执行边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批 12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的只读审查。两项双审一致；十项经第三次只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P6 执行契约 | 10 |
| P7 许可/隐私阻塞 | 2 |

十项 P6 会构造或加载工作负载，并实际进行 TVM/CUDA 编译、MetaSchedule 调优、虚拟机运行、计时、TIR 导出或结果记录；包含合成算子并不改变其执行边界。两项 P7 有私有调优工作目录或外部 ONNX 输入绑定，必须先改为路径安全、可审计的公开输入输出契约。第三方裁决确认：环境依赖本身不足以归 P5，脚本若自身承担 compile/tune/run/measure 即归 P6。

没有运行候选、模型、数据、设备、子进程或网络任务。受限 evidence 和 HMAC 不进入公开树；本批新增 12 个身份后真实唯一覆盖为 **734/1,551**。
