# P3-6：下一批候选只读审阅

状态：已完成只读审阅；本批次未迁入代码、测试、数据或生成物，未推送、未发布。

## 审阅结论

本批次按 P2b 的重新盘点边界检查三个最小候选，不复制私有文件。

1. 一个冷启动筛选模块仅依赖 Python 标准库并处理内存数值，适合未来改写；但其模块说明包含历史实验叙述，且筛选结果会原样返回调用方 label。公开版必须改用中性说明、固定位置索引或字段白名单，并以合成输入测试不回显路径或私有标识后，才能重新申请迁入。
2. 一个 Stage6 独立验证请求构造器为纯内存逻辑，但会深拷贝并返回整个调用方行，还绑定固定实验任务与后端标签。它必须先重构为匿名行 schema 和显式投影层；本批次不迁入。
3. 公开树已经存在 Stage6 协议 smoke 的等价物，因此不重复迁入。审阅同时触发了对现有公开模块的 P3-1 加固；该加固在独立复审后另行记录。

## 未改变的发布边界

没有真实数据、模型、checkpoint、engine、ONNX、硬件执行器、结果目录或 shell 脚本因本审阅进入公开树。以上“可改写”判断不是自动迁移许可；P3 仍须逐批完成相同的脱敏、测试和独立复审。
