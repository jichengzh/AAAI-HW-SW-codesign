# P3-101 TensorRT、TVM 与校准边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的双独立只读审查。主审与独立复审均先核验同一固定队列身份；10 项经第三方只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 2 |
| P6 执行契约 | 10 |

P4 项仅聚合既有实验指标或声明 DoE anchor 的外部证据约束。P6 项实际承担 TensorRT engine build/benchmark、ONNX Q/DQ build、TVM compile/tune/measure、模型量化校准或配置驱动部署构建。第三方裁决确认本地路径、GPU 编号或输出风险本身不是许可证/权限阻塞，不能用 P7 替代实际执行契约；也确认仅有环境依赖不足以把构建/执行工具降为 P5。没有运行候选、模型、数据、设备、子进程或网络任务；受限 evidence 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **986/1,551**。
