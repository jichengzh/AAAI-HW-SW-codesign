# P3-123 测量证据与凭据阻塞复审

状态：已接受（本地，含 1 项 P7 阻塞）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批完成 12 项 `migrate_document` 候选的 P2 metadata/bytes、当前内容、实际职责/输入输出/支持链、公开等价物、许可证/来源及路径、凭据、网络、身份和受控输出风险的双独立只读审查。3 项 P4/P6 真实职责分歧经第三方只读裁决；另有 1 项经独立安全复核确认存在真实凭据暴露，安全处置优先于其原有执行职责。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 8 |
| P5 环境契约 | 0 |
| P6 执行契约 | 3 |
| P7 许可或公开阻塞 | 1 |

checkpoint blocker、builder readiness、calibration、五点量化核对、native-INT8/FP16 测量 rows、energy telemetry、full-engine speed 与 output-error/AP-safe gate 的已测制品和声明边界归 P4。checkpoint recovery、多卡训练/评测、scale-aware route 修复、completion queue/runner 与批量补点的实际编排归 P6。第三方裁决确认：已完成制品、测量表、scope redline 与验证记录为主导时，后续运行计划不改变其 P4 边界。

P7 项不进入公开树：受保护记录仅标记其为真实凭据和私有运行信息暴露，未公开任何值、路径或连接细节。解除条件是确认相关旧凭据已轮换并失效、从源文档移除明文与私有路径并采用环境变量示例、完成泄露回归扫描后重新只读审阅；本批不将这些条件视为已完成。不存在经公开实现与测试/API 证明的同职责替代，故未声明迁入、改写或重复。未运行候选、模型、数据、设备、子进程或网络任务；受限 evidence、candidate identity 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **1,244/1,551**，剩余 **307** 项文档候选。
