# P3-85 Stage2 覆盖、队列与 LUT 边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批 12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的只读审查。五项双审一致；七项经第三次只读裁决。

| 处置 | 数量 |
| --- | ---: |
| 非必要私有 CLI 排除 | 1 |
| P4 外部制品契约 | 5 |
| P6 执行契约 | 6 |

排除项的纯 outlier 算法已由公开 API 和定向测试覆盖，私有文件 I/O CLI 已被既有公开范围策略明确排除。五项 P4 汇总外部测量、制品 registry、探测运行或候选计划，不执行设备或测量命令。六项 P6 会实际运行 TVM/CUDA worker、外部 telemetry/latency 命令，或生成绑定硬件资源、命令、制品和输出的执行调度队列。第三方裁决确认：只生成候选计划而没有命令链的脚本归 P4；具有可执行硬件测量调度语义的队列归 P6。

没有运行候选、模型、数据、设备、子进程或网络任务。受限 evidence 和 HMAC 不进入公开树；本批新增 12 个身份后真实唯一覆盖为 **794/1,551**。
