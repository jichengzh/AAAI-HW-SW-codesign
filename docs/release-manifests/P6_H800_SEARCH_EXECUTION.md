# P6 H800 搜索执行边界

状态：**P6.1 静态 343×2 历史路径已完成（本地）；P6.2 动态框架候选空间的离线接入与验证已完成（本地）；P6.3 历史执行适配器已实现并通过离线验证**。这些本地完成都不表示 P6 整体关闭、真实 H800 框架来源闭环成功、公开论文结果或 Stage6/Stage7 论文证据完成。

## P6.1：已完成的静态历史路径

P6.1 是已完成的本地历史路径：静态候选空间为 343 个 Pyramid 结构分别按两种精度展开，即静态 343×2。该历史收口仅确认 Git 忽略的本地执行控制器完成固定四轮反馈；它不公开候选标识、测量结果、检查点、日志或任何执行材料，也不能作为论文复现证据。

P6.1 的完成不启动 P6.2、Orin、TensorRT、P7 或 P8。

## P6.2：动态框架候选空间

P6.2 从 Stage2 动态派生候选池，不预设为 343 或 686。每个候选由其单一精度策略表示，单个候选不是 mixed-precision。动态候选空间的离线实现与验证在 Tasks 1--3 通过后仅记录为离线完成；它不替代真实运行，也不产生公开结果或论文证据。

## P6.3：历史执行适配器

P6.3 历史执行适配器已实现并通过离线验证；真实 H800 框架来源四轮执行仍待本地运行。该离线验证与 P6.2 动态候选空间、P6.1 历史收口和论文证据严格区分，不产生公开结果，也不表示 P6 整体关闭。

## 公开与匿名归档边界

公开面只记录脱敏的契约、离线验证和状态边界；不记录候选标识、执行位置、命令、结果、检查点或日志。匿名归档继续只保留允许的公开代码和配置，并排除所有 Git 忽略的本地执行配置、输出与运行材料。P6.1 历史收口、P6.2 离线完成及 P6.3 离线验证均不改变这一边界。
