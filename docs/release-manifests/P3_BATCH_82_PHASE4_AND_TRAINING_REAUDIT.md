# P3-82 Phase4 制品与训练边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批 12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的只读审查。八项双审一致；四项经第三次只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 7 |
| P6 执行契约 | 4 |
| P7 许可/隐私阻塞 | 1 |

七项 P4 校验、解析或聚合外部数据、日志、硬件结果、配置和指标制品，本身不触发训练或硬件执行。四项 P6 自身执行 LightGBM 训练、交叉验证/评估并导出新模型与指标；其数据来源须在后续执行契约绑定。P7 项带有第三方 VFE 微基准来源与改写许可风险。第三方裁决确认：外部训练数据不改变脚本自身训练、评估和模型导出的 P6 主导职责。

没有运行候选、模型、数据、设备、子进程或网络任务。受限 evidence 和 HMAC 不进入公开树；本批新增 12 个身份后真实唯一覆盖为 **758/1,551**。
