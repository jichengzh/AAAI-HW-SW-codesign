# P3-144 硬件优化设计、进展与 RALPH 提示复审

状态：已接受（本地，含 P7 阻塞）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批 12 项 `migrate_document` 候选完成 P2 分类/字节数、当前内容 SHA-256、实际职责与输入输出、调用关系、公开等价物、来源/许可证和安全输出的双重逐项审查，并经第三方逐项只读裁决。最终为 P4=9、P6=1、P7=2，无重复项。

- P4：剪枝实验反思、结果汇总、搜索空间设计、D 空间分析和硬件优化进展文档主要记录历史结果、设计解释与外部制品 provenance，不承担当前执行入口。
- P6：`实施计划_1.3_可配置硬件优化.md` 是硬件部署、benchmark、LUT 和联合搜索的执行契约。
- P7：`RALPH_PROMPT.md` 与 `RALPH_PROMPT_v2.md` 是可执行代理工作流提示，均含绝对本机路径和运行命令；须改为环境变量/无身份示例、清除私有路径并完成当前树/历史/导出物扫描后重审。

未运行候选、模型、数据、GPU、网络或外部服务；受限 evidence、逐项 decisions、候选 identity 与 HMAC key 不进入公开树。

本批新增 12 个冻结身份后，真实唯一覆盖为 **1,496/1,551**，剩余 **55** 项。P3 仍在进行中，P4 尚未开始；未推送、未发布。
