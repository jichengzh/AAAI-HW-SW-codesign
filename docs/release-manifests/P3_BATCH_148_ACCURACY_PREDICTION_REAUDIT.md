# P3-148 精度预测、主动采样与训练报告复审

状态：已接受（本地，含 P7 阻塞）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批 12 项 `migrate_document` 候选完成 P2 分类/字节数、当前内容 SHA-256、实际职责与输入输出、调用关系、公开等价物、来源/许可证和安全输出的双重逐项审查，并经第三方逐项裁决。最终为 P4=2、P6=9、P7=1，无重复项。

- P4：搜索空间体系化总结和松耦合软硬件协同是方法/概念边界说明。
- P6：精度预测实现与 SOP、sanity 评测、主动采样、PLAN-B、决策清单、训练报告、失败分析等包含 predictor/LUT 接口、数据转换、训练/评测阶段、脚本或验收流程。
- P7：完整决策清单含绝对私有 Bench2Drive 数据路径、权重下载源和 checkpoint 边界；须路径环境变量化、清理大型资产引用并完成数据/权重许可及当前树/历史/导出物扫描后重审。

未运行候选、模型、数据、GPU、网络或外部服务；受限 evidence、逐项 decisions、候选 identity 与 HMAC key 不进入公开树。

本批新增 12 个冻结身份后，真实唯一覆盖为 **1,544/1,551**，剩余 **7** 项。P3 仍在进行中，P4 尚未开始；未推送、未发布。
