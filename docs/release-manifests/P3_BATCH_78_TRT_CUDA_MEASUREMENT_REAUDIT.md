# P3-78 TensorRT/CUDA 测量边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批 12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的只读审查。三项双审一致；九项经第三次只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 2 |
| P6 执行契约 | 9 |
| P7 许可/隐私阻塞 | 1 |

两项 P4 是外部评测日志与多模型 Pareto 结果制品的解析或汇总；九项 P6 会实际构建、加载、执行或检查 TensorRT engine，并涉及 CUDA、NVML、CUDA Graph、设备采样或能耗/延迟测量。P7 项依赖第三方 NMS monkey-patch 的来源、许可证和 NOTICE 链路。第三方裁决确认：九项测量脚本即使消费外部 ONNX、engine 或校准制品，其主导职责仍是 build、execute、measure 或 inspect，不能仅归为 P4。

没有运行候选、模型、数据、设备、子进程或网络任务。受限 evidence 和 HMAC 不进入公开树；本批新增 12 个身份后真实唯一覆盖为 **710/1,551**。
