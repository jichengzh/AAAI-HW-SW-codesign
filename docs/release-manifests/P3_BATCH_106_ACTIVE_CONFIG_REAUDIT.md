# P3-106 主动采样配置边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批完成 12 项 `migrate_config` 候选的 P2 metadata/bytes、当前内容、职责/输入输出/调用链、公开等价物、许可证/来源及安全输出的双独立只读审查。主审与独立复审就 12 项的主导职责均存在分歧，已逐项交由第三方只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P6 执行契约 | 12 |

这些配置直接被剪枝、量化、评测与执行路由消费；既包括控制实际结构变换的参数，也包括量化路径的无剪枝控制面。第三方确认其主导依赖为真实执行流水线，不能仅因配置是静态 JSON 或缺少公开等价物改记为 P4。没有运行候选、模型、数据、设备、子进程或网络任务；受限 evidence 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **1,042/1,551**；后续继续按 `migrate_config` 的确定性顺序审阅。
