# P3-51 results 与 runtime 边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批从冻结 P2b 基线中确定性选取 12 个、且不在此前 374 个唯一已覆盖身份中的代码候选。所有候选完成 P2 metadata/bytes、当前内容 SHA-256、职责/调用/公开反证、许可证/来源和安全输出的只读审查。

| 处置 | 数量 | 主导责任 |
| --- | ---: | --- |
| P4 | 10 | 闭环结果/图表与 checkpoint→ONNX/engine 制品契约 |
| P5 | 1 | 分布式训练/评测运行配置的环境契约 |
| P6 | 1 | CARLA/CUDA 感知—规划全流程执行契约 |

第三次只读裁决确认两个 TRT/ONNX 配置的主导职责是把外部 checkpoint 配置为可导出、可物化的 ONNX/engine 制品输入；它们不定义驱动、设备探测或运行时准入，因此归 P4 而非 P5。闭环结果和图表候选并未运行模型或模拟器，但消费结果记录、路由元数据和图源，且会导出论文图表；它们必须在 P4 建立来源、许可、SHA-256、可发布范围和路径安全输出契约，不能直接公开迁入。CARLA/CUDA 运行时可控制车辆、采集轨迹和持久化执行输出，故归 P6。

候选源码的项目受控部分继承 Apache-2.0；该许可不覆盖 driving results、checkpoint、数据集、ONNX、engine、CARLA/CUDA/插件运行栈、传感器载荷或其派生物。后续 P4--P6 必须使用路径无关引用、显式输入契约、受控输出根和失败关闭。本批没有运行候选代码、模型、数据、设备、子进程或网络任务；受限 records 与 HMAC key 不进入公开树，HMAC ledger 已重算。本批新增 12 个不重复身份后，P3 的真实唯一覆盖为 **386/1,551**。
