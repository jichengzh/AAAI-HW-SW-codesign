# P3-64 Stage1--Stage4 遗留测试复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批 12 个此前未覆盖候选均完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开 API/test 对比、许可证/来源和安全输出的只读审查。

| 处置 | 数量 |
| --- | ---: |
| P7 阻塞 | 1 |
| 公开替代 | 2 |
| 非必要排除 | 9 |

第 4--10、12 项经第三次裁决。它们虽涉及 AP、TVM、TRT、calibration 或 repair，但均是未被当前公开支持链调用的遗留测试本身，不执行被测运行工具，不能自动转 P4--P6。第 1 项会回显未净化 probe/feature 标识，公开安全改写和测试尚不存在，故保持 P7 阻塞。第 2、11 项由公开 bridge/Stage4 selection completion 的 API 与测试替代。

本批没有运行候选、模型、数据、设备、子进程或网络任务；受限 records 与 HMAC key 不进入公开树，HMAC ledger 已重算。本批新增 12 个不重复身份后，P3 的真实唯一覆盖为 **542/1,551**。
