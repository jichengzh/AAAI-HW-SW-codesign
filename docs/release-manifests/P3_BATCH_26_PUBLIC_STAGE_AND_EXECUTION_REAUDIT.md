# P3-26：公开替代与执行边界逐项复审

状态：已完成（本地）。本批逐项读取 12 个候选的当前内容，核对其 P2 metadata/字节数、当前 SHA-256、职责、导入/调用、公开替代物、来源/许可证和安全输出。没有执行私有脚本、数据、模型、设备、子进程或生成物。

## 逐项结论

| 职责类别 | 数量 | 暂定处置 | 复审结论 |
| --- | ---: | --- | --- |
| 公开 Stage1/演示入口已覆盖的实现 | 5 | `duplicate_or_superseded` | 其中 2 项与公开树字节完全一致，3 项由公开同路径模块替代；公开替代保留 API 职责，并由 Stage1 predictor、trace graph、demo pipeline 或 smoke 测试覆盖。私有版本不再承担额外发布责任。 |
| 测量行与补全队列契约 | 2 | `external_contract_p4` | 主导输入是外部测量行、制品注册表、模型根或工作目录。P4 必须先定义资产许可、版本、SHA-256、目录投影、路径无关输出和缺失时失败信息。 |
| ONNX/TVM/TRT、源物化、反馈回合与实际执行支持 | 5 | `execution_contract_p6` | 这些候选需要生成或读取 ONNX/二进制/测量制品、绑定外部源物化证据、处理真实反馈回合，或调用系统进程。公开树只能在 P6 提供失败关闭的执行契约或报告 schema，不能默认运行。 |

逐项安全审查未发现直接凭据、邮箱或 IP。部分候选包含本机目录、结果目录、运行时或制品 provenance 信号；这些信号是进入 P4/P6 或被公开替代的原因，不能按“源码已存在”自动公开。

## 证据与阶段边界

12 项均完成当前内容 SHA-256 与 P2 metadata/字节数对账，当前内容漂移为 0。P2b inventory 没有每文件历史 digest，当前 SHA-256 仅绑定本轮读取的内容；源码变化时必须重新审计。

本批不迁入源码、测试、数据、模型、checkpoint、ONNX、engine 或实验结果。受限 HMAC evidence/decision records 仅用于本机核验，不构成 P3 关闭账本。新的发布级 re-audit 从 85/1,551 增至 97/1,551；P4 尚未开始。
