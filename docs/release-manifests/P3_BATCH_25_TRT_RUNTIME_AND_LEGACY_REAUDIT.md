# P3-25：TRT、环境清单与遗留入口逐项复审

状态：已完成（本地）。本批逐项读取 12 个候选的当前内容，核对其 P2 metadata/字节数、职责、调用关系、公开替代物、来源/许可证和安全输出。没有执行私有脚本、数据、模型、设备、子进程或生成物。

## 逐项结论

| 职责类别 | 数量 | 暂定处置 | 复审结论 |
| --- | ---: | --- | --- |
| 安全证据验证、上游评测入口与平台收口 | 3 | `external_contract_p4` | 公开树可承接硬件无关的验证/证据契约，但不具备私有输入解析、缓存揭示或完整上游运行栈。P4 必须以脱敏固定输入和路径无关输出实现该边界。 |
| 平台环境/清单验证 | 2 | `environment_contract_p5` | 需要平台相关模型或制品身份。P5 只能公开 schema、探测与失败关闭验证，不能随附真实制品或本机目录。 |
| TRT/ONNX 构建、导出和验证 | 4 | `execution_contract_p6` | 依赖模型、数据、原生插件、GPU 或生成的二进制。只能作为外部执行配方/契约，不能进入默认公开 CI 或被宣称为已复现。 |
| 原生基线与模型导出 | 2 | `blocked_license_or_permission` | 私有运行根、模型资产许可或派生输出条件尚未解决，不能公开迁入。 |
| 遗留精度预测器 | 1 | `duplicate_or_superseded` | 当前公开 Stage4 成本模型、选择和不确定性契约已覆盖其支持的复现职责；不迁入遗留默认模型加载行为。 |

## 内容漂移处理与阶段边界

4 项在入队快照后发生内容变化。它们没有继承队列中旧的内容摘要：本批重新读取当前版本，重新绑定受限 SHA-256，并复查其调用关系、来源/许可证和安全输出。其余 8 项也完成同一轮当前内容绑定。P2b inventory 不带每文件历史 digest，因此当前 SHA-256 只用于本轮内容可追溯性；以后再变化时必须重审。

本批不迁入源码、测试、数据、模型、checkpoint、ONNX、engine 或实验结果。受限 HMAC evidence/decision records 仅用于本机核验，不构成 P3 关闭账本。新的发布级 re-audit 从 73/1,551 增至 85/1,551；P4 尚未开始。
