# P3-81 预测器与 Stage1 公开链复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批 12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的只读审查。七项双审一致；五项经第三次只读裁决。

| 处置 | 数量 |
| --- | ---: |
| 已公开迁入 | 2 |
| 已安全公开改写 | 1 |
| P6 执行契约 | 5 |
| P7 许可/隐私阻塞 | 4 |

两项公开迁入与私有版本逐字节一致，并由公开 Stage1 workflow 测试支持；一项公开改写移除了私有路径、要求显式运行和输出边界，并有 release 安全测试。五项 P6 自身会执行 ONNX/TVM build/run 或训练 LightGBM 并写出模型和指标；其外部输入仍需在后续执行契约中绑定。四项 P7 仍有私有工作目录、第三方训练/算子来源或错误输出回显风险。第三方裁决确认：读取数据后自行训练、评估和导出模型的脚本属于 P6，而不只是外部制品整合。

没有运行候选、模型、数据、设备、子进程或网络任务。受限 evidence 和 HMAC 不进入公开树；本批新增 12 个身份后真实唯一覆盖为 **746/1,551**。
