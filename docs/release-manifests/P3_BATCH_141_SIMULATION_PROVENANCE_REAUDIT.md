# P3-141 仿真来源、路线 fixture 与执行计划复审

状态：已接受（本地，含 P7 阻塞）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批 12 项 `migrate_document` 候选完成 P2 分类/字节数、当前内容 SHA-256、实际职责与输入输出、调用关系、公开等价物、来源/许可证和安全输出的双重逐项审查，并经第三方逐项只读裁决。最终为 P4=6、P6=5、P7=1，无重复项。

- P4：仿真框架概览、时延/保真度反思、tau 曲线结果、根因分析、references 索引、MoE 论文维度分析和编译器调度 survey 等以论文/测量来源或外部证据边界为主，不是独立执行入口。
- P6：H800 IPC 双进程运行设计、两份不同的冻结路线 fixture，以及剪枝/量化/TRT/评测/搜索的全局执行计划承担执行输入或执行编排职责；两份路线库用途不同、内容与哈希不同，不能去重。
- P7：`study_joint_search_methods_v1.md` 含私有论文 PDF 目录和用户提供来源，外部再分发许可未确认；须移除私有路径、确认来源许可并完成当前树/历史/导出物扫描后重审。

第三方裁决确认：q6/q7 是不同的执行 fixture，不是 `duplicate_or_superseded`；q10 是本批唯一 P7 阻塞项。未运行候选、模型、数据、GPU、网络或外部服务；受限 evidence、逐项 decisions、候选 identity 与 HMAC key 不进入公开树。

本批新增 12 个冻结身份后，真实唯一覆盖为 **1,460/1,551**，剩余 **91** 项。P3 仍在进行中，P4 尚未开始；未推送、未发布。
