# P3-62 遗留执行测试边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批确定性选取 12 个此前未覆盖代码候选，逐项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开反证、许可证/来源和安全输出的只读审查。

| 处置 | 数量 |
| --- | ---: |
| 非必要排除 | 11 |
| 公开替代 | 1 |

第 1--9、11、12 项经第三次裁决。多数候选只是当前公开流程不调用的遗留测试：它们并不执行其被测目标所含的 replay、watchdog、TVM、TRT、硬件配置或子进程，不能仅因被测脚本的潜在边界而误转 P4--P6。第 12 项由公开 Stage1 autoscan、census、predictor workflow 测试承担同类合同，故为公开替代。若日后迁移对应运行工具，必须重新建立 P4--P6 契约，不得依赖本批排除。

本批没有运行候选、模型、数据、设备、子进程或网络任务；受限 records 与 HMAC key 不进入公开树，HMAC ledger 已重算。本批新增 12 个不重复身份后，P3 的真实唯一覆盖为 **518/1,551**。
