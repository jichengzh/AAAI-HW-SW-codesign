# P3-110 主动量化配置职责边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批完成 12 项 `migrate_config` 候选的 P2 metadata/bytes、当前内容、职责/输入输出/调用链、公开等价物、许可证/来源及安全输出的双独立只读审查。全部逐项边界分歧已由第三方只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P6 执行契约 | 12 |

FP32/no-quant 配对配置作为控制面决定实际剪枝或量化执行分支；INT8/混合精度配置直接控制量化模型变换和评测路径。第三方确认这些均为执行输入，静态 JSON、实验矩阵用途或缺少公开等价物均不能自动改记为 P4。没有运行候选、模型、数据、设备、子进程或网络任务；受限 evidence 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **1,090/1,551**；后续继续按 `migrate_config` 的确定性顺序审阅。
