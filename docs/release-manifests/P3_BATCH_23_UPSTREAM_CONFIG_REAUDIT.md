# P3-23：上游配置与外部物化逐项复审

状态：已完成（本地）。本批逐项读取 12 个候选的当前内容，核对其 P2 metadata/字节数、当前 SHA-256、职责、调用关系、公开替代物、来源/许可证和安全输出。没有执行私有脚本、数据、模型、设备、子进程或生成物。

## 逐项结论

| 职责类别 | 数量 | 暂定处置 | 复审结论 |
| --- | ---: | --- | --- |
| 上游协同感知训练/评测配置 | 8 | `external_contract_p4` | 代码来源具有 Apache-2.0 依据，但每项均引用外部数据集、checkpoint 或插件运行栈；公开树没有安全的完整运行等价物。P4 必须先确定资产许可、版本、校验和、目录投影和缺失时的失败信息。 |
| 校准/保留集特征收集、测量行执行、模型导出 | 4 | `execution_contract_p6` | 这些工具会加载模型或数据、物化派生输出、启动外部运行时或依赖设备；输出可能含运行时/制品 provenance。公开树只应提供失败关闭的执行契约或报告 schema，而不应默认运行。 |

代码来源的 Apache-2.0 依据不等同于外部数据集、checkpoint、框架插件或派生制品的再分发许可。逐项安全审查未发现直接凭据；但非阻塞项仍不能直接迁入，因为它们会处理外部资产或其 provenance。

## 证据与阶段边界

12 项均完成当前内容 SHA-256 与 P2 metadata/字节数对账、导入/调用审阅、公开等价物检查、来源/许可证结论和安全输出审查。P2b inventory 没有每文件历史 digest，当前 SHA-256 仅绑定本轮读取的内容；源码变化时必须重新审计。

本批不迁入源码、测试、数据、模型、checkpoint、ONNX、engine 或实验结果。受限 HMAC evidence/decision records 仅用于本机核验，不构成 P3 关闭账本。新的发布级 re-audit 从 49/1,551 增至 61/1,551；P4 尚未开始。
