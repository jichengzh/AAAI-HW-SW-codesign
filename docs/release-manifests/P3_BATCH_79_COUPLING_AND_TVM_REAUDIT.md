# P3-79 耦合与 TVM 执行边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批 12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的只读审查。五项双审一致；七项经第三次只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 1 |
| P6 执行契约 | 6 |
| P7 许可/隐私阻塞 | 5 |

P4 项仅解析外部 screening 制品。六项 P6 会构建、加载、执行或检查 TensorRT/TVM 编译与调优流程，涉及 CUDA、MetaSchedule 或测量。五项 P7 当前仍含私有模型、数据、工作目录或第三方来源/许可边界；它们必须先完成脱敏、来源与可公开输入契约。第三方裁决确认：合成算子也不等同于纯环境描述，只要实际执行 CUDA/TVM 调优、编译或运行，主导边界仍为 P6。

没有运行候选、模型、数据、设备、子进程或网络任务。受限 evidence 和 HMAC 不进入公开树；本批新增 12 个身份后真实唯一覆盖为 **722/1,551**。
