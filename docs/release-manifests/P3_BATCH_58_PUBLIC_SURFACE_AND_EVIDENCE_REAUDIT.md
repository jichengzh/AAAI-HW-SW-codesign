# P3-58 公开表面与证据边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批从冻结 P2b 基线中确定性选取 12 个、且不在此前 458 个唯一已覆盖身份中的代码候选。直接外部信号候选已完成此前优先队列，故本批按词典序审阅其余未覆盖代码职责；所有候选完成 P2 metadata/bytes、当前内容 SHA-256、职责/调用/公开反证、许可证/来源和安全输出的只读审查。

| 处置 | 数量 | 主导责任 |
| --- | ---: | --- |
| 已公开迁入 | 2 | 与公开同路径、同字节的 package marker |
| 已安全改写 | 2 | 有公开同职责、受控输出或更窄安全导出面的实现与测试 |
| 非必要排除 | 1 | 无运行表面且不在受支持公开流程中的 legacy marker |
| P4 | 7 | measurement budget、baseline/predictor search、ratio lookup 与 ablation evidence 契约 |

第 1、2、11、12 项因前两次审阅对“迁入/替代/改写/排除”的公开职责边界不同而进入第三次只读裁决。裁决确认：同字节的公开 package marker 应记为已公开迁入而非由其他实现替代；仅文档说明且无受支持调用的 legacy namespace 可排除；公开版本以测试覆盖的更窄安全 contract 替代私有 evidence/command export surface 时应记为安全改写。其余 7 项虽含 CPU 离线评分、搜索或表格处理，但主导依赖仍是外部测量、baseline、预测器、LUT 或实验结果，故归 P4；本批没有 P5/P6 项。

候选源码的项目受控部分继承 Apache-2.0；该许可不覆盖测量表、预测器、LUT、实验结果、比率文件或其派生物。后续 P4 必须提供来源/哈希、路径无关 schema、匿名 fixture、受控输出根与失败关闭。本批没有运行候选代码、模型、数据、设备、子进程或网络任务；受限 records 与 HMAC key 不进入公开树，HMAC ledger 已重算。本批新增 12 个不重复身份后，P3 的真实唯一覆盖为 **470/1,551**。
