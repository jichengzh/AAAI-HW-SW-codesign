# P3-45 Stage7 执行、反馈与证据边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4 启动授权。

## 范围与结论

本批从冻结 P2b 基线中确定性选取 12 个、且不在此前 302 个唯一已覆盖身份中的代码候选。逐项完成 P2 metadata/bytes、当前内容 SHA-256、职责/输入/输出/调用者、公开职责反证、许可证/来源和安全输出的只读检查；全部当前内容绑定通过。

| 处置 | 数量 | 主导责任 |
| --- | ---: | --- |
| P4 | 5 | actual-v3 反馈/轨迹、cache 和 formal input/evidence 外部制品契约 |
| P6 | 7 | GPU 租约、恢复生命周期、物理管线、actual-v3 执行桥、顶层编排和 schedule 控制 |

主审与独立复审对多数结论一致。三个混合边界由第三次只读裁决：GPU 租约候选不仅检查环境，也负责锁、控制器进程、重试与生命周期，主导归 P6；两个 actual-v3 适配/推进候选不启动 GPU、控制器或测量进程，只认证和推进外部反馈、物化图与轨迹状态，主导归 P4。公开树没有覆盖这些完整职责的等价实现或测试 API。

## 许可与安全边界

候选源码继承可验证的项目级 Apache-2.0，未见独立第三方许可证或来源标记；没有因源码许可证进入 `blocked_license_or_permission`。该许可不覆盖 checkpoint、ONNX、engine、cache 证据、GPU 租约、运行输出或其他外部资产。P4/P6 后续契约必须明确可得性、校验与公开许可。

候选的 GPU UUID、控制器/进程身份、部署清单、路径、artifact hash、反馈/轨迹状态和日志均可能泄露受控实验信息。任何公开实现必须使用路径无关标识、受控输出根、显式环境变量和失败关闭；不得迁入原始租约、调度、硬件或真实制品。

## 验证边界

本批只读取源码、调用关系、公开树反证和项目级许可证；没有运行候选代码、私有模型、数据、设备、子进程或网络任务。受限逐路径 evidence、decisions 和 HMAC key 不进入公开树；路径无关 HMAC ledger 已由当前 inventory 与 decisions 重算。该批新增 12 个不重复身份后，P3 的真实唯一覆盖为 **314/1,551**。
