# P3-61 遗留测试边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批从冻结 P2b 基线中确定性选取 12 个、且不在此前 494 个唯一已覆盖身份中的代码候选。所有候选完成 P2 metadata/bytes、当前内容 SHA-256、职责/调用/公开反证、许可证/来源和安全输出的只读审查。

| 处置 | 数量 | 主导责任 |
| --- | ---: | --- |
| 非必要排除 | 11 | 当前公开流程不调用的 empty/legacy pytest、私有模型/结果/执行辅助测试与展示图回归 |
| 公开替代 | 1 | 公开 Stage1 coupling predictor API 与 workflow 测试覆盖同类契约 |

第 2--5、8--12 项经第三次只读裁决。裁决区分被测私有执行器的潜在边界与“旧测试文件本身”的当前职责：旧测试不启动模型、GPU、TVM、子进程或真实测量，且不被公开支持链调用，不能仅因被测脚本含执行行为而转入 P4/P6。第 8 项例外：公开 Stage1 API 及 workflow 测试覆盖同类 predictor、blocker 和 calibration 合同，故其旧测试可记为公开替代。遗留图像回归被公开 Stage4 selection/report 契约替代，不作为论文图证据。

候选源码的项目受控部分继承 Apache-2.0；该许可不覆盖测试引用的模型、checkpoint、ONNX、parquet/CSV、GPU/TVM 结果或论文图源。若未来需要恢复任何被测能力，必须另以 P4--P6 输入/执行契约和脱敏 fixture 重建，不能复用本批排除结论。本批没有运行候选代码、模型、数据、设备、子进程或网络任务；受限 records 与 HMAC key 不进入公开树，HMAC ledger 已重算。本批新增 12 个不重复身份后，P3 的真实唯一覆盖为 **506/1,551**。
