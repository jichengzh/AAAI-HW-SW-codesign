# P3-99 Phase2 测试与调试边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

12 项完成 P2 metadata/bytes、当前 SHA-256、职责/调用、公开等价物、许可证/来源和安全输出的双独立只读审查。主审与独立复审均先核验同一固定队列身份；8 项经第三方只读裁决。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 6 |
| P6 执行契约 | 2 |
| 非必要排除 | 4 |

P4 项是 checkpoint、评测、TVM report、Orin 指标或其他外部实验制品的私有验证测试；P6 项覆盖实际 TVM measurement runner 或短时 finetune runner 的执行链。4 个一次性 CUDA/ONNX/训练状态调试探针不在受支持公开流程中，按非必要排除。第三方裁决确认没有测试已具备真实公开被测实现与 API 证据，不能提前记为“已改写”；也拒绝了不合法的“非必要排除 → P7”组合。没有运行候选、模型、数据、设备、子进程或网络任务；受限 evidence 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **962/1,551**。
