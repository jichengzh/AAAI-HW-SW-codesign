# P3-105 配置职责边界复审

状态：已接受（本地）；P3 继续进行中，不构成 P3 关闭或 P4--P6 启动授权。

本批完成 12 项 `migrate_config` 候选的 P2 metadata/bytes、当前内容、职责/输入输出/调用链、公开等价物、许可证/来源及安全输出的双独立只读审查。12 项均出现主审与独立复审的逐项边界分歧，已全部通过第三方只读裁决后才接受。

| 处置 | 数量 |
| --- | ---: |
| P5 环境契约 | 1 |
| P6 执行契约 | 10 |
| 非必要排除 | 1 |

硬件能力与工具链描述的主导职责是环境约束，归 P5。10 个主动采样剪枝参数组会作为真实模型剪枝、量化或评测流水线的输入，因此归 P6，而非仅因尚无公开等价物改记为 P4。一项不在支持链中的内部审查收据无产品调用链，按协议排除且没有后续阶段。没有运行候选、模型、数据、设备、子进程或网络任务；受限 evidence 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **1,030/1,551**；后续继续按 `migrate_config` 的确定性顺序审阅。
