# P3-124 加速证据与凭据工作流复审

状态：已接受（本地，含 2 项 P7 阻塞）；P3 继续进行中，不构成 P3 关闭或 P4--P7 启动授权。

本批完成 12 项 `migrate_document` 候选的 P2 metadata/bytes、当前内容、实际职责/输入输出/支持链、公开等价物、许可证/来源及路径、凭据、网络、身份和受控输出风险的双独立只读审查。8 项 P4/P6 真实职责分歧经第三方只读裁决；另有 2 项经独立安全复核确认保留历史凭据标记、远程访问工作流和私有运行信息，安全处置优先于原有执行职责。

| 处置 | 数量 |
| --- | ---: |
| P4 外部制品契约 | 8 |
| P5 环境契约 | 0 |
| P6 执行契约 | 2 |
| P7 许可或公开阻塞 | 2 |

FP32 energy 重测、INT8 不确定性审查、TensorCore/full-engine counterfactual、rewritten engine、跨精度根因/scale-sweep 与 AP-shape/speed-gate 的已测制品、范围和声明边界归 P4。AP-safe full-val 监控与 full-engine suite/门禁的实际执行编排归 P6。第三方裁决确认：已有测量、诊断和 claim boundary 为主导时，随后列出的 build/run/repair 计划不改变 P4；而正文已进入 full-val 监控、同步与失败处理闭环时归 P6。

两项 P7 不进入公开树：受保护记录仅标记其遗留凭据访问工作流和私有运行信息风险，未公开任何值、路径、主机、PID 或连接命令。解除条件是确认历史凭据已轮换、撤销或失效；从源文档移除或改写整个凭据/访问工作流为非可执行环境变量示例；脱敏私有路径、主机、用户与运行标识；对当前树、相关历史与导出物完成泄露回归扫描后重新只读审阅。本批不将这些条件视为已完成。不存在经公开实现与测试/API 证明的同职责替代，故未声明迁入、改写或重复。未运行候选、模型、数据、设备、子进程或网络任务；受限 evidence、candidate identity 和 HMAC 不进入公开树。本批新增 12 个身份后真实唯一覆盖为 **1,256/1,551**，剩余 **295** 项文档候选。
